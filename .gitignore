# Python
.venv/
__pycache__/
*.pyc
*.pyo
*.egg-info/

# Flask / local DB
.env
*.db
*.sqlite
*.sqlite3

# Doane launcher logs
.hub-logs/

# Node / Vite
node_modules/
client/dist/
client/tsconfig.tsbuildinfo

# Built React bundle (regenerated by vite build into app/static/).
# The whole dir is a build artifact — don't track anything in it.
# Flask's SPA fallback returns a friendly message when the dir is missing
# (pre-build dev) so this is safe.
app/static/

# Editor
.vscode/
.idea/
.DS_Store
/.vs
.vs/
*__pycache__/